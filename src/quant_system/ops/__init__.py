"""Fail-closed, operator-invoked Agent v0.2 release operations.

Nothing in this package is imported by the resident API or connector worker.
The modules expose explicit verification commands for disposable databases,
non-editable package upgrades, hermetic zero-effect proof, and launchd restart
observation.
"""
